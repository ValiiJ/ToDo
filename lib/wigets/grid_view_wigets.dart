import 'package:flutter/material.dart';

class GridViewWigets extends StatelessWidget {
  const GridViewWigets({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text('33'));
  }
}
